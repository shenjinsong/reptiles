server:
  port: 8080

spring:
  application:
    name: reptiles-server

  redis:
    host: 192.168.1.24
    port: 6379

  datasource:
    url: jdbc:mysql://192.168.1.2:3306/public?useSSL=false&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
